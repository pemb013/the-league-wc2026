# The League — WC 2026 Predictions
Self-contained front-end (`index.html`) + serverless API (`api/dashboard.js`, football-data.org).
Participant data lives at the top of `api/dashboard.js` (PARTICIPANTS). Prize pool is configured
in `index.html` (CONFIG.showPrizePool = true). Deploys on commit via Vercel.
